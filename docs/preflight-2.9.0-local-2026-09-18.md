# Локальная подготовка к сборке — 18 сентября 2026

Это не отчёт о выпуске установщиков. Ветка `codex/reliability-proposals-search`,
база `c5841e8`, версия исходников пока 2.8.9. Изменения не отправлены в GitHub;
CI и публикация релиза в этом прогоне не запускались.

## Закрытые блокеры

- Нативные сценарии нового КП с вложением/логотипом до первого сохранения и
  повторного применения отдельного шаблона завершены. Оба ZIP разобраны,
  все страницы DOCX и PDF отрисованы и визуально проверены; подробности в
  `qa-native-2026-09-18.md`, пункты 11–12.
- `check_windows_installed_contract.py` ошибочно требовал старую строку,
  которая была удалена вместе с искусственной задержкой запуска. Проверка
  теперь требует готовность backend + рабочей папки, немедленный первый
  запрос и отсутствие таймера минимального показа заставки. Проверки
  сохранности старого portable-кода, разделения installed flavor, подписи
  и остальных условий не отключены. Восемь новых регрессий проходят.
- В workflow добавлен `smoke_proposals.py`: запускает именно переданный
  исполняемый worker, не импортирует реализацию КП из исходников. Проверяет
  DOCX/PDF/PNG-preview/ZIP, суммы, логотип, состав архива, контрольные суммы,
  отсутствие внутренних данных и отказ при приватных полях в DTO.
  Включён до упаковки и после неё для macOS/Windows, результаты сохраняются
  в CI artifacts. Само изменение workflow не доказывает прохождение CI.
- Первый свежескомпилированный onefile действительно выявил ошибку,
  незаметную в исходном запуске: python-docx обращается к
  `docx/parts/../templates/default-footer.xml`, а каталог `parts` отсутствовал.
  Общий macOS/Windows build-script теперь явно включает шаблоны docx и
  текстовый marker, сохраняющий физический каталог `docx/parts`.
  Две новые регрессии воспроизводят этот отказ и проверяют комплектацию.

## Повторно выполнено локально

- Frontend: **690/690**, 93 файла, старт 16:24:40.
- Worker: **150 passed / 5 skipped** без Office-переменной;
  отдельно весь `test_proposals.py` с комплектным headless LibreOffice:
  **17/17**, включая ранее пропущенные интеграционные сценарии.
- Rust desktop: **106/106**.
- Скрипты установки/безопасного извлечения/комплектации: **26/26**
  (8 новых startup + 2 новых packaging).
- `npm run build`, TypeScript, проверка версий, Windows packaging contract,
  ruff для worker и изменённых скриптов, `git diff --check` — успешно.
- Новый smoke на исходном worker: все шесть проверок прошли. Это не
  доказательство работы скомпилированного worker.

## Свежий скомпилированный worker — результат

После исправления состава пакета создан новый macOS arm64 onefile, Nuitka
2.8.10 / Python 3.12.14. Производственные параметры упаковки сохранены,
но output/temp направлены в отдельную QA-папку; release-sidecar не заменён.

- Бинарник: `/tmp/sbk-qa290.cODEY9/compiled-worker/sbk-scanner-worker`.
- SHA-256: `956654d069892818e0926568d992fd727aa6974a60e40845ea5b7bf367db7048`.
- `scripts/smoke_proposals.py` — **6/6**: DOCX, PDF, preview, ZIP и два
  ожидаемых отказа при приватных данных в DTO. Неизбранный файл и внутренние
  PNG-метаданные не попадают в клиентский результат.
- Отчёт: `/tmp/sbk-qa290.cODEY9/compiled-proposal-smoke/run-51dh22ze/report.json`.
  Отчёт первого упавшего прогона сохранён отдельно в `run-33yo4c25`.
- Этот бинарник подключён к отдельному нативному QA-приложению. После
  перезапуска создан настоящий одностраничный PDF-preview, затем через
  системный диалог сохранён `qa-compiled-native.zip` (149089 байт).
  Логотип, сумма 1250,25, выбранное вложение и его хеш проверены; готовые
  DOCX/PDF отрисованы и просмотрены.

Для standalone smoke использован QA-runtime с ссылкой на комплектный Codex
headless LibreOffice, обнаруженный обычным поиском по resources, без Office
override. Для native QA сохранён явный путь к тому же комплектному Office.
Это подтверждает свежую компиляцию worker и его связь с приложением, **не**
финальный подписанный macOS bundle или установленный Windows-пакет.

## Границы

Тестовая папка `/tmp/sbk-qa290.cODEY9`, только синтетические данные.
Ни база пользователя, ни пароли/права доступа не менялись.
Готовые установщики Windows/macOS, реальная установка/обновление, два
физических компьютера и SMB-разрыв не проверены. Windows-часть workflow
должна действительно пройти на Windows перед выпуском.
