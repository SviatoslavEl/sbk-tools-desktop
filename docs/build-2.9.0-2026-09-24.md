# Проверка сборки 2.9.0-test.74 — 24 сентября 2026

## Результат GitHub Actions

- [Прогон 74](https://github.com/SviatoslavEl/sbk-tools-desktop/actions/runs/35985850003): **success**.
- Исходный коммит: `880c537c5f3178cb14dd470de6d6df06ed9a88d3`.
- Ветка: `codex/reliability-proposals-search`; ручной запуск без тега,
  слияния в main и публикации релиза.
- Версия приложения: `2.9.0`; обозначение пакетов: `2.9.0-test.74`.
- Начало: `2026-09-24T10:12:11Z`; завершение: `2026-09-24T11:26:58Z`.
  Общая длительность: 1 ч 14 мин 47 с.
- Собраны только macOS Apple Silicon и установщик Windows x64, без Windows portable.
- Ускорения: локальный кэш экземпляра, повторное использование преобразованного
  DOCX для экспорта/объединения и бинарная передача обычного предпросмотра.
  Схемы рабочих баз, доступ и пароли в этом изменении не менялись.

## Общие проверки

Задание `107588208684` прошло успешно, журнал независимо проверен вторым агентом.

| Набор | Результат |
| --- | ---: |
| Интерфейс, 94 файла | 699 passed |
| Python-обработчик | 173 passed, 5 skipped |
| Скрипты упаковки и измерителя | 43 passed |
| Основное приложение, Rust/Linux | 114 passed |
| Windows launcher, Rust/Linux | 2 passed |
| Windows installer helper, Rust/Linux | 15 passed |
| Всего, без повторного учёта дочерних Rust-процессов | 1 046 passed, 5 skipped |

В эти числа уже включены 11 регрессий DOCX-кэша и 8 тестов измерителя.
Это проверки измерителя, не новый замер скорости. Пять пропусков относятся
к реальному LibreOffice в тестах КП: в общем Linux-задании движок не настроен.
Отдельные пакетные проверки офисного движка выполнены в обоих платформенных
заданиях; их не следует выдавать за эти пять pytest-проверок.

Успешны проверки версий/упаковки/манифестов Windows/intelligence contract,
TypeScript/Vite, Ruff, rustfmt и Clippy с запретом предупреждений.

## macOS Apple Silicon

- Задание `107589121944`: **success**. Обработчик заново скомпилирован для arm64.
- 13 проверок сканера и 6 проверок КП прошли до упаковки и повторно внутри
  итогового `.app`. Повторные прогоны не считаются уникальными тестами.
- Проверка повторного использования DOCX доказательная: специальное
  предупреждение только из синтетического кэша присутствует в экспорте и
  объединении, хеш подготовленного PDF неизменен. Старый исходный обработчик
  не проходит этот тест; текущий локальный исходный обработчик проходит 13/13.
- Манифест 15 851 ресурса проверен до и после обработки итоговым приложением.
- Проверены целостность ad-hoc подписи и появление готового окна.
  Это не полный ручной UI-прогон. **Developer ID и нотариализации Apple нет**.
- [Скачать Mac-артефакт](https://github.com/SviatoslavEl/sbk-tools-desktop/actions/runs/35985850003/artifacts/10803513790).
- Размер внешнего ZIP: 348 093 154 байта. SHA-256 GitHub:
  `824809c53cd271bb840169573d34d3cf8854674ce679b8d6dc962974d9a226c8`.
- Внутри: `SBK-Tools-2.9.0-test.74-aarch64-apple-darwin-unsigned.app.zip`
  и файл контрольной суммы.

## Установщик Windows x64

- Задание `107589121961`: **success**. Нативные Rust-тесты: 112 passed,
  дочерние прогоны блокировки повторно не посчитаны.
- Несовпадение исходников исключило reuse прежнего worker: он скомпилирован заново.
- 13 проверок сканера и 6 проверок КП прошли до упаковки и после установки.
  Скачанные свидетельства подтверждают метку кэша в экспорте и объединении,
  объединённый документ содержит 6 страниц.
- Манифест 19 576 ресурсов проверен до упаковки, после установки и обработки.
- Пройдены установка, обновление, повторный запуск, параллельный viewer,
  штатное освобождение блокировки, сохранение блокировки после аварии,
  неизменность SQLite, удаление/переустановка с сохранением ProductData.
  Тест использует изолированную папку CI, не реальную SMB-сеть. Восстановление
  после аварии выполняется явно тестом, не через интерфейс админ-панели.
- Появление окна в CI: первый запуск 0,71 с, повторный 0,70 с,
  недоступная сетевая папка 2,44 с. Это не время открытия документов.
- [Скачать Windows-артефакт](https://github.com/SviatoslavEl/sbk-tools-desktop/actions/runs/35985850003/artifacts/10804489665).
- Внутри: `SBK-Tools-Fast-Setup-2.9.0-test.74-x64.exe`, 659 795 949 байт,
  и его `.sha256`. Подпись non-tag кандидата пропущена.
- Внешний ZIP: 659 050 046 байт. SHA-256 GitHub:
  `aef5e3078a7ee20944768805ab4e282cbade42eb5ce6cbfd1772e0be29fedfef`.
  Это хеш ZIP, не EXE. Сам EXE на локальном Mac не проверялся; установочный
  сценарий выполнен настоящим Windows runner.

## Предупреждения и границы

- npm сообщает о двух moderate-уязвимостях зависимостей. Исправление зависимостей
  не включалось в эту сборку; идентификаторы уязвимостей в CI-логе не перечислены.
- Сохраняется предупреждение о JS-блоке 899,63 КБ, исключаемых тестовых модулях
  NumPy и устаревающих Node API в GitHub artifact action.
- Кандидат не является опубликованным релизом. Измеренный ранее эффект около
  4× относится к экспорту просмотренного DOCX исходным worker на Mac; нельзя
  переносить его на первое открытие, Windows или всю программу без замеров.
- Полные журналы независимо проверены двумя агентами. Файлы общих/маковских
  логов: `/tmp/sbk-build74-audit.dYKCMc`; Windows: `/tmp/sbk-build74-windows-audit.ZYh8hu`.

## Независимая локальная проверка скачанного Mac-пакета

- Внешний ZIP скачан и его SHA-256 совпал с GitHub. Внутренний ZIP:
  356 541 757 байт, SHA-256
  `303f92bb5dabe2419742d8f14b19a9b4d59c65c7833a2395d3452ecc3f3acc7c`;
  совпадает с вложенным `.sha256`.
- Распакованный пакет: `/tmp/sbk-build74-final.QEaBE8/app/СБК Инструменты.app`.
  Подтверждены версия 2.9.0, arm64, 15 851 ресурс и ad-hoc подпись.
- Запущен именно скачанный `.app`, с `SBK_TOOLS_WORKSPACE`, указывающим на
  `/tmp/sbk-build74-final.QEaBE8/QA-ProductData`, не рабочую базу пользователя.
- Через реальные кнопки проверены загрузка синтетического DOCX и индикатор
  загрузки, страницы 2/3, переключение оригинала/обработки, пресет «Чёткий ч/б»,
  переход в «Кадры» и возврат с сохранением файла/страницы/пресета, сохранение
  через системный диалог. Масштаб 50%, полный экран и перемещение полотна
  проверены визуально: виден текст третьей страницы. Снимки окна показаны в задаче.
- Интерфейс сообщил «PDF готов», 100%. Независимая проверка результата:
  3 страницы, 127 826 байт, без шифрования. Файл:
  `/private/tmp/sbk-prepared-cache-build-preflight/run-wnk9dtfe/candidate74-ui-checked.pdf`.
- Все пять пропущенных в CI real-office pytest-проверок отдельно выполнены
  с LibreOffice из скачанного пакета: **5 passed, 0 skipped**, 22,49 с.
  Это исходные тесты renderer с комплектным офисным движком, а не новый замер
  скорости скомпилированного worker. Журналы: `/tmp/sbk-build74-officeqa.KerV4l`.
- Манифест и подпись успешно перепроверены после офисных тестов и GUI-проверки.
  Проверочный экземпляр штатно закрыт. Полный повторный ручной прогон всех
  модулей приложения и реальная SMB-сеть этим тестом не заявляются.
