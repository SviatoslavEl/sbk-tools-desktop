# Проверка 2.8.6 — 9 сентября 2026

Статус: кандидат 2.8.6, перечисленные ниже нативные проверки завершены. Релизные пакеты и публикация ещё не подтверждены. Рабочая ветка: `codex/v2.8.6-scanner-shared-lock-fixes`.

## Область

Масштабирование сканера относительно курсора, свободное перемещение листа, межпроцессная сессия редактора общей папки, компактная панель владельца и связанные UX-состояния загрузки/ошибки/блокировки.

Все ручные проверки выполняются на синтетических данных в `/tmp/sbk-ui-v286.2arGwG/ProductData`. Два отдельных приложения обозначают себя QA-MAC-EDITOR и QA-MAC-VIEWER. Это два процесса на одном Mac, не два физических компьютера и не SMB-приёмка.

## Подтверждённые проверки

- Нативное окно редактора: штатный вход с существующим тестовым паролем, имя пользователя и имя компьютера отображаются.
- Второе нативное окно: имя первого редактора видно в верхней строке и настройках; поле пароля и вход в редактирование недоступны.
- Панель владельца находится внизу настроек и по умолчанию свёрнута в одну строку.
- После раскрытия имя редактора видно до входа владельца и после него.
- Существующий тестовый пароль владельца принимается; действие отзыва без повторного пароля недоступно.
- Сворачивание и повторное раскрытие панели очищает вход владельца и пароль.
- Открытие PDF через системный диалог проверено повторно: видны имя файла и анимация загрузки; сохранение и эффекты недоступны до готовности изображения, нулевых диапазонов нет.
- Колесо масштабирует относительно курсора у левого и нижнего правого краёв листа; обратное масштабирование возвращает исходное положение. Границы 50% и 300% подтверждены в окне, соответствующая кнопка дальнейшего масштабирования недоступна.
- Полноэкранное отображение, сброс в центр/100%, перетаскивание фона и получение фокуса для стрелок проверены в нативном окне.
- Маркер создаётся и перемещается мышью. После исправления первого эффекта координаты жеста 410,360 → 650,395 совпали с границами наложения с учётом однопиксельной рамки, положение листа не изменилось.
- Ползунок маркера доведён мышью до 100%: исходные линии под ним не видны. Обычное размытие прямоугольное, размытие печати круглое. Штрих после исправления визуально заполняет всю выбранную высоту.
- Переход сканер → кадры → сканер сохранил файл simple.pdf и четыре добавленных эффекта. В кадрах выбор чекбокса активировал экспорт ровно одной выбранной карточки в режиме просмотра.
- Штатный выход первого редактора удалил только его файл сеанса. Уже открытые настройки и вошедшая панель владельца второго окна автоматически показали свободный режим; второй экземпляр вошёл обычным паролем, его имя автоматически появилось в панели.
- После штатного закрытия обоих экземпляров создана повреждённая синтетическая запись сеанса: в настройках и панели отображалась неизвестная занятость, пароль редактора/вход и просьба освободить режим недоступны. После закрытия приложения тестовая запись сохранена переименованием, восстановление вернуло свободный статус. Базы не удалялись и не переименовывались.
- Повторная сессия 17:32–17:56: экран и действия доступны. Ранее инструмент управления сообщал о блокировке, но это нельзя считать подтверждённым состоянием Mac: обнаружен и ранее созданный тестовый PDF.
- PDF с четырьмя эффектами сохранён через системный диалог: `/tmp/sbk-ui-v286.2arGwG/scanner-ui-v286-verified.pdf`, 104977 байт, одна страница A4, PDF 1.7. Страница отрендерена и визуально сверена: маркер/штрих при 100% перекрывают линии полностью, обычное размытие прямоугольное, размытие печати круглое.
- Выявлен и исправлен запрет generic-opener для PDF вне HOME/системного TEMP. После обновления процесса новый `/tmp/sbk-ui-v286.2arGwG/scanner-ui-v286-open-check.pdf` успешно открывается кнопкой в приложении «Просмотр» macOS; «Открыть папку» выделяет именно этот PDF в Finder. Общие разрешения opener не расширялись.
- Отрицательный сценарий: только синтетический PDF временно переименован в Finder, нажатие открытия показало отдельное понятное предупреждение без ложной ошибки обработки. Имя возвращено, повторное открытие сработало без повторной генерации, предупреждение исчезло. Результат и четыре эффекта сохранились.
- После заморозки исходников (без HMR) повторно проверен переход сканер → кадры → сканер: файл и четыре эффекта на месте. Нативно видны разные подписи «Непрозрачность» и «Сила размытия» с названием эффекта и страницей.
- В просмотре калькулятор пересчитал себестоимость 2500000 в цену 3934500 и прибыль 645000. После обычного входа редактора кнопки сохранения доступны, исходные подсказки настроек восстановлены. Синтетическая карточка `QA 2.8.6 — проверка сохранения` сохранена и прочитана после перезапуска со значениями 2500000 / 20%.
- При марже 100% экспорт/сохранение недоступны; поле объясняет допустимый диапазон. После исправления вместо абсурдной диаграммы показано «Исправьте исходные данные». Ввод возвращён к 20%; реальная база не затрагивалась.
- Продолжение 10 сентября: запущен локальный production-бинарник со встроенным интерфейсом `tauri://localhost` (без HMR, с изолированным QA-worker). Обычный результат сохранён как `scanner-v286-final-document.pdf`; переход в пустое объединение не показывает его как объединённый PDF. Добавлены два синтетических PDF, предпросмотр каждого работает; лист второго файла перенесён первым. Сохранён `scanner-v286-final-merge.pdf`, открыт штатной кнопкой в «Просмотре», обе страницы визуально проверены в правильном порядке. Переключение document → merge → document сохраняет отдельные результаты и метрики; после перехода в кадры и обратно объединение осталось на месте.

## Автоматические проверки

- Frontend: 241 тест в 43 файлах, включая геометрию масштабирования, состояния редактора, отрендеренные недоступные формы, свёрнутую панель, подсказки доступа, переход калькулятора из просмотра в редактор, ошибочные графики, готовый результат, начальную загрузку и принадлежность результатов режимам сканера.
- TypeScript и production frontend build: PASS (остаётся предупреждение о размере общего JS-пакета).
- macOS: локальная production-компиляция приложения без упаковки runtime. Это не готовый установочный архив.
- Обработчик документов: 60 тестов и Ruff PASS, включая растровые проверки высоты и прозрачности штриха.
- Rust: 64 теста; с `all-features` — 65 тестов; Clippy и форматирование PASS. Добавлены проверки узкой области открытия PDF, точного пути, batch/split, недоступного/заменённого файла и символических ссылок.
- Windows installer helper: 5 тестов, Clippy и форматирование PASS.
- Замороженный portable launcher: 2 регрессионных теста, Clippy и форматирование PASS; portable-пакет не выпускается.
- Извлечение неизменённого Windows-worker из проверенного installed-артефакта: 16 тестов ограниченного извлечения и проверки манифеста PASS, включены в CI/quality gate. Повторное использование требует успешного исходного задания, точного commit/run/job/артефакта, совпадения исходников и SHA/размера/PE x64. Установщик не запускается; при любом отказе выполняется полная компиляция worker.
- Контракты версий, Windows manifest/installer, intelligence, YAML и runtime regression: PASS.
- GitHub CI кандидатов `4558bb7` / `3a4c7a4` / `1de38e2`: [34362666528](https://github.com/SviatoslavEl/sbk-tools-desktop/actions/runs/34362666528) / [34363278264](https://github.com/SviatoslavEl/sbk-tools-desktop/actions/runs/34363278264) / [34367308442](https://github.com/SviatoslavEl/sbk-tools-desktop/actions/runs/34367308442), задания `checks` и `scanner-worker` PASS. Последний включает исправления открытия PDF/графиков; разделение режимов требует следующего CI.
- Полный кандидат `3a4c7a4` / `2.8.6-test.62`: [34363270973](https://github.com/SviatoslavEl/sbk-tools-desktop/actions/runs/34363270973) SUCCESS для quality gate, macOS и Windows installer. Windows install/close/crash/recovery smoke завершён с сохранностью ProductData. Первый/повторный запуск: 0.90/0.78 с; viewer: 0.34 с; после crash: 0.89 с; после явного восстановления: 0.74 с. Проверена неизменность claim после crash и его освобождение при нормальном выходе. Это проверка упаковки более раннего кандидата, не финальные пакеты новых UI/opener-исправлений.

Проверки backend включают атомарное резервирование, чужую сессию при ошибочно свободной локальной блокировке, повреждённые сведения, аварию процесса, отсутствие перехвата по паролю, сохранность чужих токенов и явное освобождение своего сеанса при выходе.

## Исправления дополнительного UI/UX-review

- При невозможности прочитать статус используется состояние «неизвестно», а не «свободно».
- Недоступные элементы оформлены визуально; длинные имена/пути переносятся, настройки переходят в одну колонку на узком окне.
- Просьба освободить режим недоступна без известного адресата.
- Поздний ответ ручного обновления не возвращает уже завершённый вход владельца.
- Выход в просмотр доступен и без настроенного пароля рабочей папки.
- Подтверждение обновления выводится после успешного чтения; при ошибке сохраняются последние сведения о резервных копиях с предупреждением.
- Перетаскивание листа переводит клавиатурный фокус на область просмотра без скачка прокрутки.
- Во время рисования/изменения эффекта или факсимиле колесо не меняет масштаб и прокрутку, чтобы не сбить геометрию активного жеста.
- Устранён сдвиг листа при появлении первой строки эффектов.
- Подсказки полей возвращаются к исходным после перехода из просмотра в редактор; исправление покрыто поведенческими тестами.
- Для действий калькулятора с собственной проверкой валидности и доступа установлен явный контракт владения `disabled`: общая граница не восстанавливает устаревшее состояние просмотра поверх нового состояния React. Невалидные расчёты остаются заблокированными.
- Толщина штриха больше не зависит от интенсивности, одинаковая геометрия используется в CSS и обработчике документа.
- Готовые PDF открываются по узкому реестру результатов текущего процесса, после проверки пути и формата, а не через расширение общих файловых разрешений. Ошибка открытия не сбрасывает готовый результат и не предлагает заново обрабатывать документ.
- Непрозрачность и сила размытия подписаны отдельно; невалидный расчёт не рисует вводящую в заблуждение диаграмму.
- В нативном окне воспроизведён перенос обычного готового PDF в пустую вкладку объединения с ошибочной подписью «Объединённый PDF готов». Результаты, прогресс, ошибки и предупреждения разделены по режимам; метрики объединения больше не меняют оценку открытого документа. Семь новых регрессий PASS; повторная нативная проверка 10 сентября PASS. Временная недоступность канала управления интерфейсом не препятствует этой выполненной проверке и не считается подтверждением блокировки Mac.

## Оставшаяся приёмка

- Повторное выполнение Windows install/close/crash/recovery smoke в CI на итоговом коммите.
- Полные установочные пакеты macOS/Windows, контрольные суммы и ссылки релиза.
- Два физических компьютера с целевой сетевой папкой: остаётся отдельной проверкой среды пользователя.
