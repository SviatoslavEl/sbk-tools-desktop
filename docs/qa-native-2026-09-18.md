# Видимая проверка новой функциональности — 18 сентября 2026

Проверялась локальная сборка из текущих исходников, не опубликованный установщик.
Отдельное приложение: `/tmp/sbk-qa290.cODEY9/SBK QA 290.app`.
Отдельная синтетическая база: `/tmp/sbk-qa290.cODEY9/ProductData`.
Рабочие данные пользователя, пароли и права доступа не изменялись.

## Пройденные сценарии

1. **Закупка:** в видимом веб-предпросмотре пустая карточка показывает «Не проверена».
   Цена 1 000 000 и затраты 700 000 дают 300 000 прибыли; изменение цены до
   1 200 000 даёт 500 000. Синтетическая карточка сохранена и открыта заново:
   производное значение осталось 500 000.
2. **Передача из калькулятора:** в нативном приложении «Создать КП» переносит
   вычисленную цену 1 639 375,00 в новую форму. Заполнены исполнитель, заказчик,
   адрес, адресат, условия, контакт, подписант и внутреннее примечание.
3. **Валидация КП:** отрицательная цена блокирует все три формата экспорта;
   нажатие сообщения возвращает к ошибочному полю. После исправления количества
   на 2,5, цены на 1000,50, скидки на 10% и включённого НДС 22% итог составляет
   2251,13, стоимость без налога — 1845,19, НДС — 405,94.
4. **Шаблон:** сохранён отдельный тестовый шаблон с собственным колонтитулом.
5. **Сохранение и экспорт КП:** карточка сохранена; настоящий PDF-предпросмотр
   показывает одну страницу. Через нативные диалоги созданы DOCX и PDF.
   Приложение перезапущено, сохранённая карточка и шаблон доступны.
6. **ZIP КП:** добавлен выбранный тестовый PDF. Первый прогон выявил старый путь
   вложения после сохранения. Исправлено применение возвращённого backend payload;
   регрессия добавлена. Повторный нативный экспорт создал ZIP с DOCX, PDF,
   одним выбранным приложением и перечнем. Никаких иных вложений нет.
7. **Контроль клиентского документа:** уникальное внутреннее примечание
   `PRIVATE_QA290_DO_NOT_EXPORT_COST_MARGIN` отсутствует в XML DOCX, тексте PDF
   и всех разобранных файлах ZIP. Итоговая PDF-страница полностью просмотрена
   после растеризации; нет обрезанного текста/таблиц, титул чёрный, единичная цена
   отображается с двумя десятичными знаками.
8. **Центр состояния:** нативный интерфейс показывает доступную тестовую папку,
   текущий экземпляр/пользователя/компьютер редактора, отсутствие копий в новой
   базе и выключенное автоматическое резервирование. В журнале видны реальные
   успешные экспорты и возникшая ошибка ZIP. Не показывается ложная отметка
   проверки целостности копии или гарантии записи по сети.
9. **Глобальный поиск:** запрос `QA290` находит сохранённое КП; нажатие результата
   открывает нужную карточку. В браузерном предпросмотре отдельно найден и открыт
   сохранённый расчёт. Поиск не отправляет данные во внешние сервисы.
10. **Выбор сертификатов:** в нативной новой кадровой карточке созданы два
    синтетических сертификата — А с PDF и Б с DOCX. Пустое назначение не позволило
    сохранить карточку; после заполнения компании, отдела и должности сохранение
    прошло. В подборе отмечен сотрудник, затем только сертификат А. Счётчик файлов
    равен 1. Готовый архив содержит только PDF сертификата А; ни DOCX Б, ни его
    реквизиты `TEST-B` не входят в `records.json`.
11. **Вложения до первого сохранения:** в новом КП `КП-20260918-7748b1`
    до первого Save добавлены PDF и логотип. До Save также создан отдельный
    шаблон с логотипом и колонтитулом. После сохранения экспортирован ZIP
    (149089 байт): DOCX, PDF, ровно одно выбранное приложение и перечень.
    Хеш приложения совпадает с перечнем; DOCX и PDF содержат логотип и итог
    1250,25. Обе одностраничные версии отрисованы и полностью просмотрены.
12. **Независимое повторное применение шаблона:** создано другое новое КП
    `КП-20260918-de1c89`, выбран сохранённый шаблон из пункта 11. Логотип и
    колонтитул восстановились, стороны и цена остались пустыми. Заполнены
    другие стороны и цена 250,00; после Save ZIP создан (88785 байт).
    В ZIP нет приложений предыдущего КП; логотип есть в DOCX и PDF,
    колонтитул и новая цена верны. DOCX и PDF отрисованы и полностью просмотрены.
13. **Свежий скомпилированный модуль:** после исправления комплектации
    python-docx нативное QA-приложение перезапущено с новым onefile вместо
    исходного Python-entrypoint. Для КП из пункта 11 через GUI созданы настоящий
    PDF-preview (1 страница) и `qa-compiled-native.zip` (149089 байт).
    Логотип, итог 1250,25, выбранное приложение и его SHA подтверждены разбором;
    DOCX/PDF отрисованы и просмотрены. Подробности компиляции и отдельного
    smoke — в `preflight-2.9.0-local-2026-09-18.md`.

## Контрольные локальные результаты

- `/tmp/sbk-qa290.cODEY9/qa-native-proposal.docx`
- `/tmp/sbk-qa290.cODEY9/qa-native-proposal.pdf`
- `/tmp/sbk-qa290.cODEY9/qa-native-proposal.zip` — итог после исправления путей.
- `/tmp/sbk-qa290.cODEY9/export-check/final-page.png` — отрисованный итоговый PDF из ZIP.
- `/tmp/sbk-qa290.cODEY9/qa-staff-selected-A.zip`
- `/tmp/sbk-qa290.cODEY9/qa-fresh-proposal-logo.zip`
- `/tmp/sbk-qa290.cODEY9/qa-template-reuse.zip`
- `/tmp/sbk-qa290.cODEY9/qa-compiled-native.zip`
- `/tmp/sbk-qa290.cODEY9/fresh-logo-check/` и `template-reuse-check/` — разбор
  ZIP, повторная отрисовка DOCX и растеризация экспортированного PDF.

Это временные QA-файлы, не клиентские документы и не установочные пакеты.

## Не выдавать за выполненную приёмку

Первый прогон прерывался блокировкой экрана Mac. После возобновления два
оставшихся сценария завершены в видимом нативном GUI — пункты 11 и 12 выше.

Не проверены готовый Windows-установщик, настоящий SMB между двумя компьютерами,
аварийный разрыв сети, масштаб ОС 125/150% и полный набор действий скринридера.
Не проводился новый ручной перебор каждой кнопки всех старых модулей.
Публикация GitHub-релиза в этот прогон не входит.
