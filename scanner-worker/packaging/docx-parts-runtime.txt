Keep this directory in compiled python-docx distributions.
python-docx 1.2.0 resolves header/footer templates through parts/../templates;
the intermediate parts directory must exist even when its modules are compiled.
